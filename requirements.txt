# Ensure pip can find the official CPU PyTorch wheels
-f https://download.pytorch.org/whl/cpu/torch_stable.html

# PyTorch CPU builds (the +cpu suffix selects CPU wheel from the index above)
torch==2.6.0+cpu
torchvision==0.21.0+cpu

# App dependencies (kept compatible with gradio 5.7.1)
transformers==4.47.0
gradio==5.7.1
Pillow==11.0.0
numpy==2.1.3
pydub==0.25.1
typing_extensions==4.12.2
requests==2.32.3
huggingface-hub==0.26.5
safetensors==0.4.5
aiofiles==23.1.0
ftfy==6.1.1
